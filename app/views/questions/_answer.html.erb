<div class="card">
  <ul class="list-group list-group-flush">
    <li class="list-group-item">回答者：<%= link_to answer.user.nickname, user_path(answer.user) %>さん（<%=answer.user.profession %>）<small>投稿日<%= @question.created_at.strftime('%Y年%m月%d日 %H:%M')%></small></li>
    <li class="list-group-item"><big> A </big><%= answer.text %></li>
  </ul>
</div>