<div class="container">
  <div class="row ">
    <div class="col-xs-10 col-lg-5">
      <h3>アカウント情報の登録</h3>
      <small>全ての項目を入力してください。<br>サイト内ではニックネーム，年齢，性別，職業以外は表示されません。</small>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

    <div class="field">
      <%= f.label :"氏名" %><br />
      <%= f.text_field :name, placeholder: '氏名を入力', :class=>"form-control" %>
    </div>

    <div class="field">
      <%= f.label :"ニックネーム※" %><br />
      <%= f.text_field :nickname, placeholder: 'ニックネームを入力', :class=>"form-control" %>
    </div>

    <div class="field">
      <%= f.label :"生年月日※" %><br />
      <%= f.date_field :birth_day , :class=>"form-control"%>
    </div>

    <div class="field">
      <%= f.label :"性別" %><br />
        <%= f.radio_button :sex, "男" %> <%= f.label :sex_1, "男性" %>
        <%= f.radio_button :sex, "女" %> <%= f.label :sex_2, "女性" %>
    </div>


    <div class="field">
      <%= f.label :"職業※" %><br />
        <%= f.radio_button :profession, "開業医" %> <%= f.label :profession_1, "開業医" %>
        <%= f.radio_button :profession, "勤務医" %> <%= f.label :profession_1, "勤務医" %>
        <%= f.radio_button :profession, "歯科衛生士" %> <%= f.label :profession_1, "歯科衛生士" %>
        <%= f.radio_button :profession, "歯科技工士" %> <%= f.label :profession_1, "歯科技工士" %>
        <%= f.radio_button :profession, "歯科助手" %> <%= f.label :profession_1, "歯科助手" %>
   </div>

    <div class="field">
      <%= f.label :"勤務医院" %><br />
      <%= f.text_field :office_name , :class=>"form-control"%>
    </div>

    <div class="field">
      <%= f.label :"メールアドレス" %><br />
      <%= f.email_field :email, autofocus: true , placeholder: 'メールアドレスを入力', :class=>"form-control"%>
    </div>

    <div class="field">
      <%= f.label :password %>
      <% if @minimum_password_length %>
      <small>(<%= @minimum_password_length %> 文字以上)</small>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "off" , :class=>"form-control"%>
    </div>

    <div class="field">
      <%= f.label :"password 確認" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" , :class=>"form-control"%>
    </div>

    <div class="actions">
      <%= f.submit "登録" ,:class=>"btn secondary" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
  </div>
